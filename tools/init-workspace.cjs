const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const type=process.argv[2];if(!['demo','blank'].includes(type))throw Error('使用 demo 或 blank');const dest=process.env.QIUZHAO_WORKSPACE||path.join(root,'.local-workspace');if(fs.existsSync(dest))throw Error('目标目录已存在，为避免覆盖请指定新的 QIUZHAO_WORKSPACE');require('./copy-tree.cjs').copyTree(path.join(root,type==='demo'?'examples/demo-workspace':'templates/blank-workspace'),dest);console.log('已创建工作区：'+dest);
