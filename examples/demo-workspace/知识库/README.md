# 完整虚构知识库

【完全虚构的演示资料】以下人物、在校/任职关系、成绩、获奖和日期由程序示例编造；真实机构名称仅作场景，不能用于真实投递。

包含 74 份按原知识库分类与用途对应的资料文件。结构化JSON保留原字段树并替换值，核心教育、实习、项目、语言、奖励另外生成连贯内容。

运行时以 profile.json 为唯一有效输入，避免多个历史文件互相覆盖。对应清单.json 列出逐项关系。手机号均为无效占位，邮箱为保留域；没有伪造证书、盖章文件或可用证件。
