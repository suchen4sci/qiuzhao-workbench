const {test}=require('node:test');const assert=require('node:assert/strict');const {fillTextControl,confirmedAge}=require('../src/text-control.cjs');
function mock(){let value='',calls=[];return {calls,evaluate:async f=>f({tagName:'INPUT',type:'text',maxLength:100,readOnly:false,disabled:false,getAttribute:()=>null,value,validity:{valid:true}}),fill:async()=>{calls.push('fill');value=''},press:async key=>{if(key==='Backspace')value=''},click:async()=>{},pressSequentially:async s=>{calls.push('keyboard');value=s},page:()=>({waitForTimeout:async()=>{}})}}
test('direct input failure falls back to keyboard and verifies',async()=>{const l=mock();const r=await fillTextControl(l,'项目经理');assert.deepEqual(l.calls,['fill','keyboard']);assert.equal(r.attempts[1].status,'verified')});
test('unknown date does not become a fabricated birthday or stale age',()=>{assert.equal(confirmedAge({age:51,age_confirmed_on:'2026-09-20'},'2026-09-20'),51);assert.equal(confirmedAge({age:51,age_confirmed_on:'2026-09-20'},'2027-09-20'),undefined)});
